{
  "name": "pawsitive-cat-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for Firebase: Stripe payments, charity routing, push notifications.",
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth,storage",
    "deploy": "firebase deploy --only functions",
    "seed": "npm run build && node lib/seed.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
